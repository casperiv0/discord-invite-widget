{
  "name": "discord-invite-widget",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler publish",
    "format": "prettier --write \"./**/**/*.{js,jsx,ts,mjs,tsx,md,css,json}\" --ignore-path .gitignore",
    "start": "wrangler dev --remote --env development"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20230419.0",
    "@types/node": "^20.3.1",
    "prettier": "^2.8.8",
    "typescript": "^5.0.4",
    "wrangler": "^3.0.0"
  },
  "dependencies": {
    "discord-api-types": "^0.37.45",
    "satori": "^0.10.1"
  }
}
